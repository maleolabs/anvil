package cmd

// Happy-path wiring is already provided via standard_sync auto-offer
// (list/install detect empty index and offer sync) and init --framework
// interactive prompt (implemented in project init). This file documents
// the wiring point for sto:install-happy-path.
