{
    "description": "Violates §4.2 / required field: the manifest is missing the identity field entirely. A manifest that is missing required fields makes the artifact invalid; the runtime rejects it before any lifecycle operation. Without identity, the artifact has no content-derived identity — content-derived identity cannot be established.",
    "id": "negative-missing-identity",
    "contractVersion": "1.0.0",
    "manifestLocation": {
        "path": ".anvil/manifest.json",
        "format": "json"
    },
    "verificationEvidence": {
        "verifiedAt": "2026-08-04T12:00:00Z",
        "verifier": "anvil-runtime-v2.0.0",
        "algorithm": "sha-256"
    }
}
