{"context":"Release orchestration requires knowing what is true at any given moment: which release is active, what stage a release is in, whether an activation succeeded or failed, and what the system's overall condition is. This is state.\n\nWithout explicit state management, the following problems emerge:\n\n**Implicit state.** The only way to determine the system's condition is to inspect its environment — which directories exist, what symlinks point where, what files are present. This makes state observation unreliable and error-prone. Two operators may draw different conclusions from the same inspection.\n\n**Duplicated state.** Multiple subsystems track overlapping state independently. One subsystem knows which release is active; another subsystem knows; neither is authoritative. When they disagree, t Source: ADR-006-state-management.md Date:2026-07-19 Status:Accepted.","decision":"Anvil adopts a centralized conceptual State Management model.\n\nState is a first-class architectural concept owned by the Core. Every significant platform object has a well-defined state that is observable, intentional, and authoritative.\n\nThe model distinguishes four fundamental concepts that MUST remain architecturally distinct:\n\n- **Configuration** represents intent — what should happen\n- **State** represents reality — what has happened and what currently is\n- **Lifecycle** defines valid transitions — what can happen next\n- **Artifact** represents deployable content — the thing being managed\n\nState is the system's authoritative record of reality. It is not configuration. It is not a lifecycle definition. It is not an artifact. It is the observable, persistent condition of every tracked e","consequences":"### 9.1 Positive Consequences\n\n**Consistent orchestration.** Every subsystem operates on the same state model. There is no ambiguity about the current condition of any tracked entity. Orchestration decisions are based on authoritative state.\n\n**Reliable rollback.** Rollback requires knowing which release was previously Active. Explicit state guarantees this information is always available. Without explicit state, rollback depends on environment inspection, which is unreliable.\n\n**Predictable execution.** Because state transitions follow defined lifecycle rules, the outcome of any operation is predictable. Operators know what to expect when they invoke a command.\n\n**Unified observability.** All state is accessible through a consistent model. Monitoring tools, dashboards, and CLI commands al","alternativesConsidered":"Considered\n\n### 10.1 Distributed Subsystem State\n\n**Description:** Each subsystem manages its own state independently. The Release Engine tracks release state, the Artifact System tracks artifact state, the Pipeline Orchestrator tracks pipeline state — each with its own store, format, and access model.\n\n**Rejected because:** Distributed state creates coupling between subsystems through implicit dependencies. The Release Engine needs to know artifact state to determine whether a release can be ac"}