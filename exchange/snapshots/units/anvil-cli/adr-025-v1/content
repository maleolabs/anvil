{"context":"- v1.x is a monorepo: engine, in-repo framework packages (`internal/laravel`, `internal/flutter`, `cmd/laravel-adapter`, `cmd/flutter-adapter`), wiki, and planning corpus all build and release from the Core repository; framework knowledge ships from Core (§1.1).\n- The v2 target state separates repositories: Core repository + one repository per first-party delivery lifecycle standard (§2.2).\n- The repository model (§4.1) is explicit: `maleolabs/anvil` (Core), `maleolabs/anvil-standard-laravel`, `maleolabs/anvil-standard-flutter`, then gated community namespaces.\n- The Core repository contains exactly three things: the Anvil Runtime, the delivery lifecycle specification, and Governance (§4.2).\n- ADR-025 is delegated to settle the extraction list, per-repo CI/release responsibilities, and the Source: ADR-025-repository-split-core-vs-standards.md Date:2026-08-03 Status:Proposed.","decision":"1. Repository split per §4.1: the Core repository (Anvil Runtime + delivery lifecycle specification + Governance) and one repository per first-party standard, named `anvil-standard-\u003cframework\u003e` (`anvil-standard-laravel`, `anvil-standard-flutter`), under the `maleolabs` namespace initially.\n2. What leaves Core (§6.2): `internal/laravel`, `internal/flutter`, `cmd/laravel-adapter`, `cmd/flutter-adapter`, framework build templates, framework config defaults. These become delivery lifecycle standard content (§6.3). What remains in Core is the list in §6.1.\n3. The delivery lifecycle specification stays authored in the Core repository (§5.2): governance single-threading (a contract change is a Core-scale event, Manifesto §3.6), conformance co-location (the conformance harness validates the engine","consequences":"- Build, CI, and release pipelines are split; the installer and discovery flow move to registry-based resolution (Phase 5 gate).\n- Core becomes purely engine + specification + governance; the import graph is the evidence of framework-freeness (success criterion 2).\n- Standard repositories own their content, quality, and release cadence (§4.3).\n- v1.x users: in-repo adapter builds are removed at Phase 5 (§12.1); executable resolution is unchanged by default (§12.2).","alternativesConsidered":"Considered\n\n- **Keep the monorepo.** Rejected: independent release, versioning, and maintenance are v2 requirements (§2.2, §4.7); community standards need a reproducible template.\n- **A separate specification repository.** Rejected for v2: §5.2 records four reasons; the §11.2 objective trigger governs any future re-home.\n- **Extract framework packages but keep building the standard executables from Core.** Rejected: in-repo adapter builds are removed (§12.1); standards build from their own repos"}