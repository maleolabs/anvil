{"context":"- Drift finding D1 (HIGH): framework knowledge is embedded in Core — `engine.KnownFrameworks`, framework build templates, the `NewFrameworkProjectConfig` switch, framework config defaults (§3.2).\n- The Manifesto (§3.5) states that the Core knows that a lifecycle exists, not what any framework's lifecycle contains; adding a framework must never require changing the Core. §3.6 assigns contract ownership to Core and knowledge ownership to framework content.\n- ADR-002 and ADR-009 promised the contract/knowledge separation; A3 completes it.\n- The transition plan (§12.1) records that framework knowledge removal is an intentional, governed breaking change: `init --framework` requires a registry-installed delivery lifecycle standard (at A3 completion, Phase 4). §12.2 delegates the standard-missing Source: ADR-026-core-framework-free-completion.md Date:2026-08-03 Status:Proposed.","decision":"1. Remove ALL framework knowledge from the Anvil Runtime (Core): `engine.KnownFrameworks`, framework build templates, the `NewFrameworkProjectConfig` switch, framework config defaults (D1). This covers template content (A10) and config knowledge; the extraction list is §6.2.\n2. Framework discovery and config extension become standard-driven: `anvil init --framework \u003cname\u003e` resolves the standard through the registry (A3, A4); templates and config extensions are generated from the installed standard (A10).\n3. Standard-missing failure semantics: an explicit framework declaration with no installed standard HARD-FAILS with an actionable remediation (for example: install the standard), per §12.1 — `init --framework` requires a registry-installed standard. Projects without a framework declaration","consequences":"- `anvil init --framework` fails with an actionable message when the standard is not installed; installing the standard resolves it.\n- New frameworks never require a Core change — only a delivery lifecycle standard.\n- v1.x breaking at A3 completion (Phase 4): `init --framework` requires a registry-installed standard (§12.1).\n- Non-breaking surfaces: lifecycle commands, the `anvil.yaml` schema, and projects without framework declarations are unaffected (§12.2).\n- The import graph becomes the enforcement evidence; the governance review checks it (§11.2).","alternativesConsidered":"Considered\n\n- **Graceful fallback** when a standard is missing — degrade to a generic lifecycle without framework content. Rejected: silent degradation hides a missing distribution step and contradicts the explicit-over-implicit principle (Manifesto §3.10); §12.1 requires a registry-installed standard.\n- **Partial removal** (remove templates but keep `KnownFrameworks`). Rejected: any framework knowledge in Core is a defect (§5.8); the import graph must be provably framework-free.\n- **Keep the st"}