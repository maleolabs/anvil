{"context":"Gate 2 flow `anvil standard install` membaca supported contract majors dari compatibility matrix record (ADR-029 §3; T-010 reviewer finding G4: never hardcoded). Resolusi path saat ini cwd-relative (`docs/specification-corpus/compatibility-matrix.json`) atau env ANVIL_COMPATIBILITY_MATRIX — di luar checkout repo Core, install selalu gagal di gate 2. Ini salah satu dari 4 prasyarat manual dalam keluhan UX install standards.\n\nRegression aktif: specification corpus (termasuk compatibility-matrix.json) TERHAPUS dari repo pada commit 82ddc03 ('chore: migrate to EKA KMS and remove all docs', 2026-08-25) — kini bahkan menjalankan CLI dari repo root pun gagal; unit tests skip diam-diam saat matrix absen (cmd/standard_install_test.go) dan e2e script masih menunjuk path yang mati (scripts/e2e/migration_paths_e2e.sh).\n\nPelajaran struktural: input runtime tidak boleh tinggal di docs tree. Precedent embed sudah ada di codebase (internal/skills/skills.go meng-embed core skills). Spec corpus tetap di-author di repo (ADR-029 dual publication); yang berubah hanya cara runtime MENGKONSUMSI record matrix-nya.","decision":"1. Compatibility matrix record DI-EMBED ke binary anvil (go:embed) pada build time; embedded copy adalah default resolusi supported contract majors di runtime.\n2. Precedence resolusi baru: flag/env override (ANVIL_COMPATIBILITY_MATRIX) \u003e embedded default. Default cwd-relative DIHAPUS.\n3. Embedded record divalidasi strict seperti file (document_id, contract_version, supported_majors); record corrupt/unreadable tetap abort install dengan actionable error — supported majors tidak pernah silently defaulted (PM binding decision 3 tetap berlaku).\n4. Versi matrix yang dipakai tercatat di installed-standard record dan output --json agar auditabel.\n5. Sumber kebenaran authoring tetap spec corpus di repo (ADR-029 dual publication tidak berubah); embed di-refresh oleh build CI dari corpus — binary release memakai snapshot matrix saat build, bukan fetch runtime.","consequences":"**Positive:** prasyarat install berkurang satu (tidak ada lagi env var/cwd trick untuk matrix); regression corpus-terhapus tertutup permanen; offline/airgap install jalan tanpa setup; konsisten dengan precedent embed yang ada (internal/skills).\n\n**Negative:** binary membawa snapshot matrix yang bisa tua sampai rebuild berikutnya — mitigasi: env override untuk testing/enterprise, matrix version tercatat di setiap installed-standard record sehingga auditabel matrix mana yang dipakai saat adopsi, dan CI me-refresh embed dari corpus pada setiap build release. Duplikasi sumber (corpus authoring vs embedded snapshot) dikelola oleh build pipeline, bukan manual.","alternativesConsidered":"- **Hosted matrix endpoint (URL fetch saat runtime).** Ditolak: menambah network dependency dan infra untuk data statis kecil; melawan semangat ADR-030 (no core-operated service).\n- **Hardcode supported majors di kode Go.** Ditolak: persis apa yang dilarang T-010 reviewer finding G4 — engine dan corpus akan drift.\n- **Re-add file corpus ke repo + dokumentasikan syarat cwd.** Ditolak: akar masalahnya adalah runtime input yang tinggal di docs tree — sudah terbukti mati satu kali (commit 82ddc03); syarat 'run dari repo root' adalah friction yang dikritik user."}