{"changes":["Root cause: SetHelpFunc captured cmd.OutOrStdout() (resolved writer) and cmd.SetOut(out) pinned it onto the CHILD command permanently. Test harness re-points root's output per execution; a pinned child kept writing to the stale buffer -\u003e empty stdout for every later help/Run path through that command. 183 deterministic ./cmd failures; v2.4.0 release workflow red (go test ./... gate).","Fix: drop the SetOut swap entirely — renderHelp takes an explicit io.Writer, so render into a private buffer and emit via output.Container(s, ...) to the style's raw writer; children keep inheriting root's writer.","Root help surfaces unified: SetHelpFunc routes rootCmd -\u003e writeDomainHelp (extracted from printDomainHelp) so bare anvil / --help / anvil help share one renderer (equality tests).","Margin-tolerant test assertions: verify [PASS] prefix + install Release ID extraction TrimSpace; adapter/standard/skill domain-group parsers match header names instead of indent widths."],"role":"implementation","summary":"Fix release blocker: SetHelpFunc writer pinning redirected later output to stale buffers","tests":["go vet ./... PASS","go test ./cmd -count=1 PASS (42.8s, fully green — first time since theme work)","go test ./... : all packages ok except spikes/local-deploy-race TestAC1 flaky under parallel load (passes isolated; pre-existing timing sensitivity, unrelated to presentation)","Bisect evidence: v2.3.2 PASS, 6c1716b PASS, 4ca3ba7 FAIL (183) — regression introduced by adopt-help-container merge"]}