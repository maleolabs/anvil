{"changes":["cmd/status.go human path now via styleFor(cmd).W (newMarginWriter Margin 2), JSON via s.Raw() bypass — Width fallback 0 verified, TERM dumb honored","Infrastructure from previous sto already provides marginWriter.Container.Style with s.W wrapping + Raw bypass; help via Container already enforces vertical padding","Documented codemod pattern for remaining cmd/*: replace OutOrStdout direct with styleFor + s.W/s.Raw — lint check ready"],"role":"implementation","summary":"Enforce global margin via Style.W (human) + Raw (JSON) — status migrated, help already enforced","tests":["go test ./cmd -run TestSystemInspect PASS","go test ./internal/output PASS","go vet ./cmd PASS","status human output via s.W shows Margin 2, JSON via Raw without margin verified"]}