{"description":"Spike 3 — Race local-vs-CI concurrent deploy + state locking. Validasi tidak ada state drift jika local dan CI deploy bersamaan ke target yang sama.","investigationNotes":"AC:\n- AC1: Simulasi concurrent deploy (local + CI mock) — hanya satu yang menang, state tetap deterministic, tidak ada dual-active (ADR-003 only one active).\n- AC2: State locking / optimistic reject terbukti — deploy kedua ditolak atau queued dengan error jelas, tidak corrupt `/.anvil` state.\n- AC3: Idempotency: retry deploy yang kalah tidak create duplicate release.\n- AC4: Dokumentasi rekomendasi guard: dev env allow local, prod require allowlist/confirm prompt (input untuk ADR nanti).\nTimebox: 3 hari, depends on spike 2 artifact. Evidence: concurrent run logs + state dumps before/after + lock behavior proof.","conclusion":""}