{"description":"Spike 1 — SSH transport latency \u0026 retry. Validasi distribusi 50 artifact push via SSH (scp/rsync) ke VPS dummy single target, ukur p50/p95 latency, retry idempotency jika putus di tengah.","investigationNotes":"AC:\n- AC1: 50 artifact (tar.gz + manifest.json) berhasil push via SSH scp dengan p95 \u003c 30s per artifact (network lab, size ~50MB).\n- AC2: Retry setelah simulasi disconnect (kill mid-transfer) idempotent — re-push tidak corrupt, checksum manifest.json valid.\n- AC3: SSH auth via ssh-agent / key-path dari anvil.yaml tidak expose secret di log.\n- AC4: Laporan latency histogram + failure mode classification (auth fail, timeout, partial write).\nTimebox: 3 hari, parallel dengan spike lain. Evidence: histogram CSV + retry log + checksum verify script.","conclusion":""}