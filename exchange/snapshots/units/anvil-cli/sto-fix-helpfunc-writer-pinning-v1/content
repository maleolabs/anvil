{"description":"SetHelpFunc captures cmd.OutOrStdout() then cmd.SetOut(out) pins the RESOLVED writer onto the child command permanently. Second executeCommand creates fresh buffers on root, but children keep writing to the stale pinned buffer -\u003e empty stdout, 183 test failures, release workflow red. Fix: drop the SetOut swap entirely (our renderHelp takes an explicit io.Writer, unlike cobra's template-based default), letting children keep inheriting root's writer.","acceptanceCriteria":"AC1: go test ./cmd -count=1 fully PASS (parity with v2.3.2). AC2: go test ./... green. AC3: manual CLI outputs unchanged (help/margin/colors intact)."}