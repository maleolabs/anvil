{"description":"Implementasi `anvil deploy --target \u003cenv\u003e` CLI di cmd/deploy: flags --target required, --dry-run (build+verify only), --json (envelope v1), --confirm (staging/prod). Reuse internal/artifact.Package, VerifyArtifact, internal/output (AppError, envelope, PlainStepReporter) konsisten spike UX RunDryRun.","acceptanceCriteria":"AC1: --dry-run build+verify tanpa install, human+JSON konsisten (ValidateHumanJSONConsistency). AC2: --help terdokumentasi flags+exit codes. AC3: error mapping ke AppError dengan guidance. Tests harness + go vet PASS."}