{"description":"Guard per-env + audit binding: dev allow local, staging allow --confirm, prod CI-only default + allowlist + confirm prompt + audit DeployUser binding ke SSH principal (bukan string spoofable), HMAC hash-chain audit 0600.","acceptanceCriteria":"AC1: dev deploy tanpa confirm PASS, staging tanpa --confirm REJECT, prod allowlist enforcement. AC2: audit JSON-lines 0600 HMAC chain, redacted. AC3: guard_recommendation.md diperbarui dari spike race. AC4: no RBAC bypass."}