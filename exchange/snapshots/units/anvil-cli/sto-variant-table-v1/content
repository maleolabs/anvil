{"description":"Parity Table primitive: rewrite internal/output/table.go jadi Style-aware NewTable(s, headers...).AddRow(cells, colors).Render() ke s.W dengan header Accent + dim ─ underline (IconLine), utf8 width, per-cell color, Width aware, tidak pakai dash ASCII.","acceptanceCriteria":"AC1: Table header Accent, underline Dim IconLine, rows via s.W dengan Margin 2. AC2: per-cell color via Style methods, Width fallback aware. AC3: existing table tests updated, non-TTY plain."}